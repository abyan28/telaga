# Rencana T3.2 + T5.10 — Pindah nama ke profil, login email/HP, NUPTK

Tanggal: 2026-07-16
Status: SELESAI 2026-07-16 — 70 tests passed (236 assertions), npm build OK, migrate:fresh+seed+DemoSeeder OK.
Keputusan user (2026-07-16):
1. Password awal guru: NIG → **NUPTK** (wajib). Revisi rules.md §1.5.
2. Login: **email ATAU no. HP**. Admin buat guru cukup dg HP (email boleh kosong);
   guru isi email sendiri belakangan di CMS guru. HP & email dua-duanya bisa login.
3. NUPTK **wajib** (menggantikan NIG sebagai password awal).
4. (T3.2) `users.name` → `users.username` (fallback display, diisi email/HP);
   nama asli pindah ke `guardians.nama` & `teachers.nama`.

## Prasyarat & urutan
T3.2 dikerjakan DULU (fondasi), lalu T5.10. Satu rangkaian, satu verifikasi akhir.

## Perubahan skema (migrate:fresh — data hanya demo, dev)
- `users`: rename `name`→`username`; + `no_hp` (nullable, unique); `email` jadi
  nullable + unique (guru bisa tanpa email saat dibuat). Login identifier:
  username(=email/HP saat auto-gen) tak dipakai; login pakai email ATAU no_hp.
- `guardians`: + `nama` (string). `no_hp` sudah ada.
- `teachers`: + `nama` (string), + `nuptk` (string, unique). `no_hp` sudah ada,
  jadikan **required+unique** (dipakai login & wajib saat auto-akun).
  `nomor_induk_guru`: DIPERTAHANKAN (nomor internal, boleh tetap ada; bukan lagi
  password). ponytail: tidak dihapus — masih dipakai di banyak view/test; hapus
  = diff besar tanpa manfaat. NUPTK ditambah berdampingan.

## Login email-atau-HP (AuthController)
- Input tunggal "email / no. HP" + password.
- Deteksi: kalau `filter_var($input, FILTER_VALIDATE_EMAIL)` → cari users.email;
  else cari users.no_hp. Lalu `Auth::attempt(['id_users'=>$u->id_users, password])`
  via `Auth::attempt` dg kolom yg cocok. ponytail: satu helper resolve→attempt,
  bukan custom guard.

## Auto-akun guru (T5.10, MasterDataController@storeTeacher)
- Form: nama, no_hp (wajib), nuptk (wajib), email (opsional), TTL, riwayat, kelas.
- Buat user: username = email ?? no_hp; email = input (nullable); no_hp = input;
  password = Hash(nuptk); role guru.
- Teacher: nama, nuptk, no_hp, nomor_induk_guru (opsional sekarang), dll.
- updateTeacher & guru CMS: guru boleh update email sendiri (T2.3 nyerempet, tapi
  scope minimal: cukup di updateTeacher admin + form guru).

## Titik sentuh (hasil grep — semua akan disesuaikan)
Controller: RegisterController (name→username+guardian.nama), AuthController (login),
  MasterDataController (storeTeacher/updateTeacher/teachers search pakai teachers.nama),
  AccountController (name→username; admin tetap pakai email login).
Model: User (fillable name→username,+no_hp; helper displayName()), Guardian(+nama),
  Teacher(+nama,+nuptk).
Migration: users, guardians, teachers (ALTER baru; fresh).
Seeder: DatabaseSeeder, DemoSeeder (name→username+profil.nama; guru +nuptk).
Factory: UserFactory (name→username).
Views: login.blade (field email→"email/HP", signup tetap nama→guardian),
  layouts/dashboard (`$authUser->name`→displayName), guru/dashboard & wali/dashboard
  (auth()->user()->name → displayName), admin/data/teachers (user?->name→teacher->nama,
  +field nuptk/email), registration-detail (wali?->name→guardian?->nama),
  students/student-profile/registrations/daftar-ulang (guardian?->user?->name →
  guardian?->nama), profile.blade (g->user?->name → g->nama), accounts (adm->name→username).
Tests: semua User::create(['name'=>..]) → 'username'; storeTeacher test +nuptk;
  + test login via no_hp; + test password guru = nuptk.

## displayName() helper (User)
`return $this->guardian?->nama ?? $this->teacher?->nama ?? $this->username;`
Satu sumber untuk semua view. ponytail: ganti semua `->name` view dg `->displayName()`
(atau langsung profil.nama di tempat yg sudah punya relasi profil).

## Revisi dokumen (setelah kode hijau)
- rules.md §1.5 (password guru = NUPTK), §1.6 (nama di profil, users=username+email+no_hp),
  §2.2 catatan login HP.
- notes.md: T3.2 [DONE], T5.10 [DONE]; T2.3 sebagian (guru update email) — catat.
- tasklist.md: Fase D baru + progress.

## Verifikasi
`php artisan test` hijau + `npm run build` + `migrate:fresh --seed` + `db:seed DemoSeeder`.
Login manual: admin (email), guru (no_hp + password=nuptk), wali (email).
